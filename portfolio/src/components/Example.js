import React from 'react';

const styles = {
    h1: {
        color: 'blue',
    }
};

function Example() {
    return (
       <h1 style={styles.header}>Example PAge</h1>
    );
};

export default Example;